# Copy to .env.local for local development against AWS Cognito + RDS.
# Leave unset to use guest mode (browser localStorage only).

# AWS_REGION=eu-west-2
# COGNITO_USER_POOL_ID=
# COGNITO_CLIENT_ID=
# DATABASE_URL=postgresql://user:pass@host:5432/papertrading
